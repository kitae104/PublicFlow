# 화면 캡처 넣는 방법

이 문서 하나만 보고 스크린샷을 찍어 사이트에 올릴 수 있도록 만들었습니다.
HTML을 직접 고칠 필요는 없습니다.

## 요약 (3단계)

1. 스크린샷을 찍는다.
2. 정해진 이름으로 `public/assets/img/` 폴더에 저장한다.
3. 아래 명령을 실행한다.

```
cd "D:/Github/n8n_WS/PublicFlow"
python tools/apply_captures.py
```

끝입니다. 페이지를 열어보면 자리표시자 문구가 사라지고 방금 넣은 이미지가 보입니다.

## 저장 위치

```
D:\Github\n8n_WS\PublicFlow\public\assets\img\
```

이 폴더 하나에만 넣으면 됩니다. 하위 폴더를 만들지 마세요.

## 파일명

**어떤 그림인지는 파일명으로 정해집니다.** 아래 "전체 촬영 목록" 표의 `id` 열을 그대로 파일명으로 쓰고, 뒤에 확장자만 붙입니다.

예: `[그림 1-2] 새 워크플로우 만들기`의 id가 `fig-d1-01-2`이면 파일명은

```
fig-d1-01-2.png
```

- id 철자와 대소문자를 그대로 씁니다 (전부 소문자).
- 확장자는 `.png`, `.jpg`, `.jpeg`, `.webp` 중 하나만 됩니다.
- 대문자가 섞여도(`Fig-D1-01-2.PNG`) 일단 인식은 되지만, 실행 결과에 "파일명 대소문자가 규칙과 다릅니다" 경고가 뜹니다. 경고가 뜨면 파일명을 소문자로 바꿔서 다시 저장하세요.
- 어떤 그림의 id가 무엇인지 모르겠으면 아래 "전체 목록 확인" 명령으로 다시 볼 수 있습니다.

## 권장 형식

- 형식: PNG 권장 (스크린샷 도구 기본값이 보통 PNG입니다)
- 크기: 가로 1200~1600px 정도면 충분합니다. 너무 작으면 글자가 안 보이고, 너무 크면 페이지가 무거워집니다.
- **개인정보·API 키·토큰이 화면에 그대로 찍히지 않도록 가리고 찍으세요.** 특히 아래 목록 중 "키 값은 가려서 촬영"이라고 적힌 항목은 반드시 모자이크나 검은 띠로 가린 뒤 저장하세요.

## 넣는 방법

1. 파일을 위 이름 규칙대로 `public/assets/img/`에 저장합니다 (같은 이름이 이미 있으면 덮어씁니다).
2. 아래 명령을 실행합니다.

```
cd "D:/Github/n8n_WS/PublicFlow"
python tools/apply_captures.py
```

3. 실행 결과에 `N개 삽입`이라고 나오면 성공입니다. 브라우저에서 해당 페이지를 열어 확인하세요.

**주의: 반드시 `python`으로 실행하세요. `python3`는 이 컴퓨터에서 아무 일도 하지 않는 껍데기 명령입니다.**

## 다시 찍었을 때 (재촬영)

사진이 마음에 안 들어 다시 찍었다면, **같은 파일명으로 덮어쓰고** 다시 같은 명령을 실행하면 됩니다.

```
python tools/apply_captures.py
```

## 이미지를 지웠을 때 (되돌리기)

이미지 파일을 지우고(또는 다른 곳으로 옮기고) 다시 명령을 실행하면, 그 그림은 자동으로 원래의 "촬영 안내" 문구로 되돌아갑니다. HTML을 손으로 고칠 필요 없이 다시 찍기 전 상태로 돌아갑니다.

```
python tools/apply_captures.py
```

파일명을 그대로 유지하면 이전 이미지를 새 이미지로 갈아끼웁니다. 이미지가 중복으로 쌓이지 않습니다.

## 전체 목록 확인

지금까지 몇 개를 찍었고 몇 개가 남았는지 보려면:

```
python tools/apply_captures.py --list
```

`상태` 열이 `완료`면 이미 이미지가 들어간 것이고, `대기`면 아직 찍을 사진입니다.

## 파일명을 잘못 넣었는지 확인

`public/assets/img/`에 넣은 파일 중 어떤 그림에도 해당하지 않는(오타 등) 파일이 있는지 확인하려면:

```
python tools/apply_captures.py --check
```

## 촬영 시 참고사항

- 캡처를 넣어도 사진 아래 있던 "캡처 위치: …" 안내 문구는 사라지지 않습니다. HTML 소스 안에 주석으로 남아서, 나중에 재촬영할 때 무엇을 다시 찍어야 하는지 잊지 않도록 해 줍니다. 화면에는 보이지 않습니다.
- 이미지 하나를 넣을 때마다 사이트 전체가 깨지지 않았는지 확인하려면 다음 명령도 함께 실행하세요.

```
python tools/check_site.py
```

`OK: ...`로 끝나면 정상입니다.

## 전체 촬영 대상 목록

| id (파일명) | 페이지 | 캡션 | 촬영 안내 |
|---|---|---|---|
| `fig-d1-01-1` | `public/day1/01.html` | [그림 1-1] 완성된 모습 | 캡처 위치: 노드 2개가 연결된 캔버스 전체 |
| `fig-d1-01-2` | `public/day1/01.html` | [그림 1-2] 새 워크플로우 만들기 | 캡처 위치: 우측 상단 버튼 주변 |
| `fig-d1-02-1` | `public/day1/02.html` | [그림 2-1] 완성된 모습 | 캡처 위치: 값 가져오기 노드의 표현식 입력창과 오른쪽 결과 미리보기 |
| `fig-d1-03-1` | `public/day1/03.html` | [그림 3-1] 완성된 모습 | 캡처 위치: 새 행이 추가된 구글 시트 화면 전체 |
| `fig-d1-04-1` | `public/day1/04.html` | [그림 4-1] 완성된 모습 | 캡처 위치: 발송된 접수 안내 메일함 화면 |
| `fig-d1-05-1` | `public/day1/05.html` | [그림 5-1] 완성된 모습 | 캡처 위치: 캘린더에 등록된 처리 마감 일정 화면 |
| `fig-d1-06-1` | `public/day1/06.html` | [그림 6-1] 완성된 모습 | 캡처 위치: Gemini가 생성한 안내문 초안 결과 화면 |
| `fig-d1-07-1` | `public/day1/07.html` | [그림 7-1] 완성된 모습 | 캡처 위치: IF 노드로 갈라진 두 개의 처리 경로 전체 |
| `fig-d1-08-1` | `public/day1/08.html` | [그림 8-1] 완성된 모습 | 캡처 위치: 완성된 민원 입력 폼 화면 전체 |
| `fig-d2-p1-1` | `public/day2/p1.html` | [그림 P1-1] 완성된 모습 | 캡처 위치: 텔레그램으로 도착한 공문서 요약 메시지 |
| `fig-d2-p2-1` | `public/day2/p2.html` | [그림 P2-1] 완성된 모습 | 캡처 위치: 아침에 도착한 민원 브리핑 메일 화면 |
| `fig-d2-p3-1` | `public/day2/p3.html` | [그림 P3-1] 완성된 모습 | 캡처 위치: 시트에 기록된 분류 결과와 답변 초안 |
| `fig-d2-p4-1` | `public/day2/p4.html` | [그림 P4-1] 완성된 모습 | 캡처 위치: 텔레그램으로 도착한 아침 날씨 알림 |
| `fig-prep1-1` | `public/prep/day1.html` | [그림 준1-1] 개인 Gmail 계정으로 로그인한 상태 | 캡처 위치: 구글 계정 로그인 후 우측 상단 프로필 아이콘과 이메일 주소가 보이는 화면 |
| `fig-prep1-2` | `public/prep/day1.html` | [그림 준1-2] AI Studio 로그인 후 첫 화면 | 캡처 위치: aistudio.google.com/apikey 접속 직후 화면 전체, 좌측 메뉴 포함 |
| `fig-prep1-3` | `public/prep/day1.html` | [그림 준1-3] API 키 만들기 버튼 | 캡처 위치: API 키 만들기 버튼과 그 주변, 클릭 전 상태 |
| `fig-prep1-4` | `public/prep/day1.html` | [그림 준1-4] 발급된 API 키와 복사 버튼 | 캡처 위치: 발급 직후 키 문자열 전체와 복사 버튼(키 값은 실제로는 가려서 촬영) |
| `fig-prep1-5` | `public/prep/day1.html` | [그림 준1-5] API 상세 페이지의 활용신청 버튼 | 캡처 위치: 기상청 단기예보 API 상세 페이지에서 활용신청 버튼이 보이는 화면 전체 |
| `fig-prep1-6` | `public/prep/day1.html` | [그림 준1-6] 마이페이지에서 인증키가 표시된 화면 | 캡처 위치: 공공데이터포털 마이페이지에서 발급된 인증키(서비스키) 문자열이 보이는 화면, 메뉴 위치 포함 |
| `fig-prep2-1` | `public/prep/day2.html` | [그림 준2-1] 검색 결과에서 공식 BotFather 계정 | 캡처 위치: 검색창에 BotFather 입력 후 나오는 검색 결과 목록, 확인 배지가 보이도록 |
| `fig-prep2-2` | `public/prep/day2.html` | [그림 준2-2] 봇 이름과 사용자명을 입력하는 대화 | 캡처 위치: BotFather와의 대화창에서 이름 입력, 사용자명 입력, 성공 메시지까지 이어지는 부분 |
| `fig-prep2-3` | `public/prep/day2.html` | [그림 준2-3] BotFather가 발급한 토큰 메시지 | 캡처 위치: 토큰 문자열이 포함된 BotFather의 마지막 안내 메시지(토큰 값은 실제로는 가려서 촬영) |
| `fig-prep2-4` | `public/prep/day2.html` | [그림 준2-4] 내 봇에게 메시지를 보낸 대화창 | 캡처 위치: 내가 만든 봇과의 대화창, 내가 보낸 메시지가 보이도록 |
| `fig-prep2-5` | `public/prep/day2.html` | [그림 준2-5] getUpdates 결과 화면에서 chat id 위치 | 캡처 위치: 브라우저에 표시된 getUpdates 응답 전체, "chat":{"id": 부분에 표시나 동그라미 |

이 표는 `python tools/apply_captures.py --list` 출력과 항상 같은 내용입니다. 최신 상태(완료/대기)는 명령을 직접 실행해서 확인하세요.
